# Runtime Backlog

## Purpose

This backlog records adjacent updated-roadmap work discovered during sprint `I1-S1` and intentionally not implemented in this sprint.

## Deferred Adjacent Items

1. `I1-S2` prompt/schema metadata library alignment for `M-050`.
2. `I1-S3` bounded runtime control trace ledger alignment.
3. `I1-S4` bounded metadata-slice integration review under the updated roadmap package.
4. Optional later separation of `capability_tags` from shared capability/action-class storage, if a later approved sprint requires distinct persistence semantics.

## Explicit Non-Backlog Items

The following remain out of scope until a later explicitly approved phase:

- broad autonomous agents
- LLM execution loops
- external execution
- tender submission
- EDS/signature flows
- supplier email automation

## Alignment Note

These items are documented here instead of being implemented now to preserve the updated roadmap's anti-drift rule.
