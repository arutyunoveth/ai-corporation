# Hermes recovery

## Status

`EXPERIMENTAL_INTERNAL`

The local checkout contained a complete `src/modules/hermes_agent/` package, internal router, category profiles, and focused tests. It was compiled and the focused tests passed as part of R0.02.

Hermes is opt-in only:

```text
AI_CORP_HERMES_ENABLED=false
AI_CORP_HERMES_BASE_URL=http://127.0.0.1:8099
```

It is not part of the required tender analysis flow, is not advertised by the public site, and has a deterministic fallback when disabled.

## Recovery source

- Recovery commit: `4041ac1`
- Full pre-R0 preservation: `/Users/master/Documents/arvectum-r0-backups/20260711-145008`

The recovered code is retained without a claim of production deployment or client validation.
