services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ai_corporation
      POSTGRES_USER: ai_corporation
      POSTGRES_PASSWORD: ai_corporation
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

